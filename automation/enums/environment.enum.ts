export enum Environment {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  // Extend here as new environments are provisioned — no other files need changing
  QA = 'qa',
  STAGING = 'staging',
  UAT = 'uat',
}
